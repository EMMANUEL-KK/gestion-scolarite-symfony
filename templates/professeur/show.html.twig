{% extends 'base.html.twig' %}

{% block title %}Professeur{% endblock %}

{% block body %}
    <h1>Professeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professeur.id }}</td>
            </tr>
            <tr>
                <th>Nomp</th>
                <td>{{ professeur.nomp }}</td>
            </tr>
            <tr>
                <th>Prenomp</th>
                <td>{{ professeur.prenomp }}</td>
            </tr>
            <tr>
                <th>Emailp</th>
                <td>{{ professeur.emailp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_professeur_index') }}">back to list</a>

    <a href="{{ path('app_professeur_edit', {'id': professeur.id}) }}">edit</a>

    {{ include('professeur/_delete_form.html.twig') }}
{% endblock %}
